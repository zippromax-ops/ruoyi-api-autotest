[pytest]
# 测试用例目录
testpaths = testcases

# 运行参数：详细输出、关闭输出捕获、Allure 报告输出目录（reports 已被 gitignore）
addopts = -v -s --alluredir=reports/allure-results

# 自定义标记（避免未注册标记告警）
markers =
    login: 登录模块测试
    user: 用户管理模块测试
    p0: 高优先级用例（P0）
    p1: 中优先级用例（P1）
